import type {Metadata}from'next';import Link from'next/link';import{createClient}from'@supabase/supabase-js';export const metadata:Metadata={title:'Explore the United States | LocalStreetShop',description:'Explore U.S. local businesses by state, city and street on LocalStreetShop.',alternates:{canonical:'https://www.localstreetshop.com/countries/united-states'}};export const revalidate=600;const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{auth:{persistSession:false,autoRefreshToken:false}});export default async function Page(){const{data:c}=await supabase.from('countries').select('id').eq('slug','united-states').maybeSingle();const{data:states}=c?await supabase.from('provinces').select('id,name,slug').eq('country_id',c.id).order('name'):{data:[]};return <main className="min-h-screen bg-slate-50 px-4 py-12"><div className="mx-auto max-w-6xl"><Link href="/" className="font-bold text-blue-700">← Home</Link><section className="mt-6 rounded-[2rem] bg-gradient-to-br from-blue-800 to-slate-800 p-8 text-white"><p className="text-sm font-bold uppercase tracking-[.2em] text-blue-100">LocalStreetShop United States</p><h1 className="mt-3 text-4xl font-black">Explore local America by street</h1><p className="mt-4 max-w-3xl text-white/90">New cities and streets are added organically as verified U.S. businesses join.</p><Link href="/countries/united-states/add-business" className="mt-6 inline-flex rounded-full bg-white px-6 py-3 font-black text-blue-800">Add a U.S. Business</Link></section><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{(states||[]).map(s=><Link key={s.id} href={`/countries/united-states/${s.slug}`} className="rounded-3xl border bg-white p-6 shadow-sm hover:-translate-y-1"><h2 className="text-2xl font-black">{s.name}</h2><p className="mt-2 text-slate-600">Explore cities and local streets →</p></Link>)}</section></div></main>}